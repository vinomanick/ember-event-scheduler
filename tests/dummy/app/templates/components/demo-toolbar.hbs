{{#docs-demo as |demo|}}
  {{#demo.example name='docs-demo-toolbar.hbs' class="hide-container overflow--scroll"}}
    {{#event-scheduler options=options
      onSchedulerLoad=(action "onSchedulerLoad")
      onCalendarRefresh=(action "onCalendarRefresh")
      as |scheduler|}}

      {{#scheduler.toolbar as |toolbar|}}
        {{toolbar.left}}
        {{toolbar.right}}
        {{toolbar.center}}
      {{/scheduler.toolbar}}

    {{/event-scheduler}}
  {{/demo.example}}

  {{demo.snippet 'docs-demo-toolbar.hbs'}}
  {{demo.snippet 'demo-toolbar.js' label='component.js'}}
{{/docs-demo}}
