import eventScheduler from 'dummy/utils/event-scheduler';
import { module, test } from 'qunit';

module('Unit | Utility | event scheduler', function() {
  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = eventScheduler();
    assert.ok(result);
  });
});
