<div class="calendar__row__name" data-test-es="resource">
  {{#if hasBlock}}
    {{yield}}
  {{else}}
    <div class="resource-name">{{resource.name}}</div>
  {{/if}}
</div>
<div class="calendar__row__grid" data-name="resource" data-test-es= "resource-grid" data-resource-id={{resource.id}}>
</div>